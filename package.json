{
  "name": "pi-dorsia-status",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Five-row custom footer for pi — agent/session/work/sessions/control lanes, Nord palette, Orca session + PR surfacing, hyper/prism auto-routed model display",
  "keywords": ["pi-package", "pi-extension", "pi", "statusline", "footer", "orca"],
  "repository": { "type": "git", "url": "https://github.com/Elomi-inc/pi-dorsia-status.git" },
  "files": ["src", "README.md", "LICENSE"],
  "engines": { "node": ">=22.18" },
  "pi": { "extensions": ["./src/index.ts"] },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "@types/node": "^24",
    "typescript": "^5.9",
    "vitest": "^3"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  }
}
